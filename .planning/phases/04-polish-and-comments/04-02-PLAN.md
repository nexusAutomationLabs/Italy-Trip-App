---
phase: 04-polish-and-comments
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/app/(app)/layout.tsx
  - src/components/layout/AppSidebar.tsx
  - src/components/layout/MobileTabBar.tsx
  - src/app/(auth)/login/page.tsx
  - src/components/auth/LoginForm.tsx
  - src/app/globals.css
autonomous: true
requirements: []

must_haves:
  truths:
    - "Desktop users see a fixed 240px sidebar with trip name, Itinerary and Map navigation links"
    - "Mobile users see a bottom tab bar with Itinerary and Map tabs"
    - "Login page shows split layout with Tuscany photo on left and form on right (desktop)"
    - "Login page stacks photo on top as banner on mobile"
    - "Active sidebar/tab link is visually distinct with terracotta color"
    - "Sidebar shows user display name and sign-out option at bottom"
  artifacts:
    - path: "src/components/layout/AppSidebar.tsx"
      provides: "Fixed sidebar for desktop with nav links and user section"
      contains: "AppSidebar"
    - path: "src/components/layout/MobileTabBar.tsx"
      provides: "Bottom tab bar for mobile with Itinerary and Map tabs"
      contains: "MobileTabBar"
    - path: "src/app/(app)/layout.tsx"
      provides: "Restructured layout with SidebarProvider"
      contains: "SidebarProvider"
    - path: "src/app/(auth)/login/page.tsx"
      provides: "Split-screen login page"
      contains: "Berwick goes to Tuscany"
  key_links:
    - from: "src/app/(app)/layout.tsx"
      to: "src/components/layout/AppSidebar.tsx"
      via: "import and render inside SidebarProvider"
      pattern: "AppSidebar"
    - from: "src/app/(app)/layout.tsx"
      to: "src/components/layout/MobileTabBar.tsx"
      via: "import and render for mobile"
      pattern: "MobileTabBar"
---

<objective>
Restructure the app layout with a fixed sidebar on desktop and bottom tab bar on mobile, and redesign the login page with a split-screen Tuscany photo layout.

Purpose: Per D-14/D-15, navigation moves from the top header to a sidebar (desktop) and bottom tabs (mobile). Per D-10/D-11/D-13, the login page gets a premium split-screen design. These layout changes affect every page and must be in place before other UI redesigns.
Output: New AppSidebar, MobileTabBar components, restructured (app) layout, redesigned login page.
</objective>

<execution_context>
@/Users/adamdalton/Documents/Coding Projects/Italy Trip App/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adamdalton/Documents/Coding Projects/Italy Trip App/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-polish-and-comments/04-CONTEXT.md
@.planning/phases/04-polish-and-comments/04-UI-SPEC.md

<interfaces>
<!-- Current app layout being restructured -->
From src/app/(app)/layout.tsx:
```typescript
// Currently imports Header and renders in a flex-col layout
// Must be restructured to use SidebarProvider + AppSidebar
// Auth check pattern: getUser() + redirect('/login') — PRESERVE this
// force-dynamic export — PRESERVE this
// isAdmin check via NEXT_PUBLIC_ADMIN_EMAIL — PRESERVE this
```

From src/components/ui/sidebar.tsx (installed in Plan 01):
```typescript
// shadcn sidebar exports: SidebarProvider, Sidebar, SidebarContent, SidebarGroup,
// SidebarGroupContent, SidebarGroupLabel, SidebarHeader, SidebarFooter,
// SidebarMenu, SidebarMenuItem, SidebarMenuButton
```

From src/app/(auth)/login/page.tsx:
```typescript
// Currently: full-bleed background image with centered Card
// Redesign: split layout — left photo, right form
// LoginForm is imported from @/components/auth/LoginForm
```

From src/components/layout/Header.tsx:
```typescript
export function Header({ user, isAdmin }: HeaderProps)
// Shows app name, user display name, admin badge, sign-out button
// Will be RETIRED — functionality moves to sidebar (desktop) and top bar (mobile)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sidebar layout shell and mobile tab bar</name>
  <read_first>
    - /Users/adamdalton/Documents/Coding Projects/Italy Trip App/src/app/(app)/layout.tsx
    - /Users/adamdalton/Documents/Coding Projects/Italy Trip App/src/components/layout/Header.tsx
    - /Users/adamdalton/Documents/Coding Projects/Italy Trip App/src/components/ui/sidebar.tsx
    - /Users/adamdalton/Documents/Coding Projects/Italy Trip App/src/app/globals.css
    - /Users/adamdalton/Documents/Coding Projects/Italy Trip App/src/lib/auth/actions.ts
  </read_first>
  <action>
1. Create `src/components/layout/AppSidebar.tsx` — a client component using shadcn sidebar primitives.

Structure per D-14 and UI-SPEC:
- 240px fixed width (use shadcn Sidebar default or override via CSS `--sidebar-width: 240px` in globals.css)
- SidebarHeader: Trip title "Berwick goes to Tuscany 2026" in Playfair Display (`font-heading`) 18px italic, trip dates "May 7 - 16, 2026" below in 12px muted-foreground
- SidebarContent with SidebarGroup containing SidebarMenu:
  - "Itinerary" link using `CalendarDays` icon from lucide-react, href="/itinerary"
  - "Map" link using `Map` icon from lucide-react, href="/map"
  - Each link: 40px height, 16px horizontal padding, full-width
  - Active link detection: use `usePathname()` from `next/navigation`, compare with href
  - Active state: terracotta left border 3px (`border-l-3 border-primary`), primary text color, `bg-primary/10` background, font-weight 700
  - Inactive state: muted-foreground text, no border, font-weight 400
- SidebarFooter: User section with:
  - Avatar circle (32px) — for now use colored initial circle (Avatar component from shadcn with AvatarFallback showing first letter of display_name). Avatar image support comes in Plan 04.
  - Display name text (14px)
  - Admin badge if isAdmin (same badge style as current Header)
  - Sign Out button (ghost variant, using `signOut` server action from `@/lib/auth/actions`)

Props: `{ user: User; isAdmin: boolean; profile: { display_name: string | null; avatar_url: string | null } | null }`

2. Create `src/components/layout/MobileTabBar.tsx` — a client component for bottom tab navigation on mobile.

Structure per D-15 and UI-SPEC:
- Fixed bottom-0, full width, white/card background, border-top
- Two tabs: "Itinerary" (`CalendarDays` icon) and "Map" (`Map` icon)
- Each tab: 44px height, flex-1 width, flex-col centered (icon + label)
- Active tab: primary (terracotta) icon + label color
- Inactive tab: muted-foreground icon + label color
- Active detection via `usePathname()`
- Add `pb-[env(safe-area-inset-bottom)]` for iOS safe area
- Only render on mobile: wrap in a div with `className="lg:hidden"`

3. Restructure `src/app/(app)/layout.tsx`:
- Keep: `export const dynamic = 'force-dynamic'`, auth check via `getUser()`, `redirect('/login')`, `isAdmin` check
- Add: Fetch user profile from Supabase: `const { data: profile } = await supabase.from('profiles').select('display_name, avatar_url').eq('id', user.id).single()`
- Replace Header with SidebarProvider + AppSidebar structure:
  ```tsx
  import { SidebarProvider } from '@/components/ui/sidebar'
  import { AppSidebar } from '@/components/layout/AppSidebar'
  import { MobileTabBar } from '@/components/layout/MobileTabBar'

  return (
    <SidebarProvider>
      <AppSidebar user={user} isAdmin={isAdmin} profile={profile} />
      <main className="flex-1 min-w-0 overflow-hidden pb-16 lg:pb-0">
        {children}
      </main>
      <MobileTabBar />
    </SidebarProvider>
  )
  ```
- The `pb-16 lg:pb-0` on main ensures content doesn't hide behind the mobile tab bar
- Remove the Header import (file can remain for reference but is no longer rendered)

4. Add sidebar width CSS variable to `src/app/globals.css` inside `:root`:
```css
--sidebar-width: 240px;
```
  </action>
  <verify>
    <automated>cd "/Users/adamdalton/Documents/Coding Projects/Italy Trip App" && grep -q "SidebarProvider" src/app/\(app\)/layout.tsx && grep -q "AppSidebar" src/components/layout/AppSidebar.tsx && grep -q "MobileTabBar" src/components/layout/MobileTabBar.tsx && grep -q "CalendarDays" src/components/layout/AppSidebar.tsx && grep -q "usePathname" src/components/layout/AppSidebar.tsx && npx tsc --noEmit 2>&1 | head -20 && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/layout/AppSidebar.tsx exists and exports AppSidebar
    - src/components/layout/AppSidebar.tsx contains "CalendarDays" and "Map" icon imports
    - src/components/layout/AppSidebar.tsx contains "usePathname" for active link detection
    - src/components/layout/AppSidebar.tsx contains "Berwick goes to Tuscany 2026"
    - src/components/layout/AppSidebar.tsx contains "signOut" import from auth actions
    - src/components/layout/MobileTabBar.tsx exists and exports MobileTabBar
    - src/components/layout/MobileTabBar.tsx contains "lg:hidden"
    - src/components/layout/MobileTabBar.tsx contains "safe-area-inset-bottom" or equivalent
    - src/app/(app)/layout.tsx contains "SidebarProvider" import
    - src/app/(app)/layout.tsx contains "AppSidebar" import
    - src/app/(app)/layout.tsx contains "MobileTabBar" import
    - src/app/(app)/layout.tsx does NOT contain "Header" import from layout/Header
    - src/app/(app)/layout.tsx contains "profile" query from supabase profiles table
    - src/app/globals.css contains "--sidebar-width: 240px"
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>App layout restructured with fixed sidebar on desktop and bottom tab bar on mobile, replacing the top header navigation</done>
</task>

<task type="auto">
  <name>Task 2: Login page split-screen redesign</name>
  <read_first>
    - /Users/adamdalton/Documents/Coding Projects/Italy Trip App/src/app/(auth)/login/page.tsx
    - /Users/adamdalton/Documents/Coding Projects/Italy Trip App/src/components/auth/LoginForm.tsx
    - /Users/adamdalton/Documents/Coding Projects/Italy Trip App/src/app/(auth)/signup/page.tsx
    - /Users/adamdalton/Documents/Coding Projects/Italy Trip App/src/app/globals.css
  </read_first>
  <action>
Redesign the login page per D-10, D-11, D-12, D-13 and UI-SPEC.

Rewrite `src/app/(auth)/login/page.tsx`:

**Desktop layout (lg+):** Side-by-side grid (`grid grid-cols-2 min-h-svh`):
- Left half: Full-bleed Tuscany vineyard/countryside photo (use a high-quality Unsplash URL, e.g. `https://images.unsplash.com/photo-1523531294919-4bcd7c65e216?w=1920&q=80&auto=format&fit=crop` — Tuscan vineyard). Styled as `background-image`, `bg-cover`, `bg-center`, full height.
  - Overlay: semi-transparent dark gradient (`bg-gradient-to-t from-black/60 to-black/20`)
  - Text overlay at bottom: "Berwick goes to Tuscany 2026" in Playfair Display (`font-heading`), 28px, weight 400 (regular, not bold — decorative display per UI-SPEC), italic, white text. Position with `absolute bottom-12 left-8 right-8`.
  - Below title: "May 7 - 16, 2026" in 14px, white/80, Montserrat
- Right half: Cream background (`bg-background`), centered vertically and horizontally. Contains:
  - "Welcome back" heading in 18px Montserrat bold
  - LoginForm component (existing, keep as-is — email + password fields + submit)
  - Below form: "Don't have an account?" link to /signup in 12px muted-foreground
  - Max-width constraint on form: `max-w-sm mx-auto`

**Mobile layout (below lg):**
- Photo banner at top: 200px height (`h-[200px]`), same vineyard image, `bg-cover bg-center`, with overlay and title text
- Below banner: form section on cream background, padded, same content as desktop right half

Per D-12: No social login buttons — keep email + password only. The existing LoginForm component handles this already.
Per D-11: Title is "Berwick goes to Tuscany 2026" (not "Tuscan Curator").

Also update `src/app/(auth)/signup/page.tsx` with the same split-screen treatment for consistency:
- Same left photo / top banner
- Right side: "Join the trip" heading instead of "Welcome back"
- SignUpForm component instead of LoginForm
- Below form: "Already have an account?" link to /login

Remove the Card/CardHeader/CardContent wrapper from login page — the split layout replaces the centered card approach.
  </action>
  <verify>
    <automated>cd "/Users/adamdalton/Documents/Coding Projects/Italy Trip App" && grep -q "Berwick goes to Tuscany 2026" src/app/\(auth\)/login/page.tsx && grep -q "grid" src/app/\(auth\)/login/page.tsx && grep -q "font-heading" src/app/\(auth\)/login/page.tsx && grep -q "Welcome back" src/app/\(auth\)/login/page.tsx && npx tsc --noEmit 2>&1 | head -20 && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(auth)/login/page.tsx contains "grid grid-cols-2" or equivalent for desktop split layout
    - src/app/(auth)/login/page.tsx contains "Berwick goes to Tuscany 2026" as display title
    - src/app/(auth)/login/page.tsx contains "font-heading" for the Playfair Display title
    - src/app/(auth)/login/page.tsx contains "Welcome back" heading
    - src/app/(auth)/login/page.tsx does NOT contain Card/CardHeader/CardContent imports (removed)
    - src/app/(auth)/login/page.tsx contains a vineyard Unsplash image URL
    - src/app/(auth)/login/page.tsx has mobile treatment with "h-[200px]" or similar banner height
    - src/app/(auth)/signup/page.tsx updated with matching split-screen layout
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Login and signup pages show split-screen Tuscany photo layout on desktop and stacked banner layout on mobile, with group-specific branding</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- Desktop browser at /itinerary shows sidebar with Itinerary and Map links
- Mobile browser at /itinerary shows bottom tab bar
- /login shows split-screen layout on desktop, stacked on mobile
- Sidebar active state correctly highlights current page
</verification>

<success_criteria>
App navigation uses a fixed sidebar on desktop and bottom tab bar on mobile. Login page has a premium split-screen design with Tuscany imagery. All layouts are responsive and TypeScript compiles cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/04-polish-and-comments/04-02-SUMMARY.md`
</output>
